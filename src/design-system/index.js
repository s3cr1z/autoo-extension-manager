/**
 * Public entry point for the Extension Manager design system.
 *
 * Import tokens, components, and the ThemeProvider from here:
 *
 *   import { Button, Card, lightTheme, ThemeProvider } from "@/design-system"
 */

export * from "./tokens"
export * from "./components"
export { ThemeProvider, useTheme } from "./ThemeProvider"
